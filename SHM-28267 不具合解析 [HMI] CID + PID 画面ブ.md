SHM-28267 不具合解析 \[HMI] CID + PID 画面ブラックアウト事象サマリStatus : Root cause found■ 1. 現在の状況2/5 本事象は バンガロールのテスト環境にて断続的に再現可能2/5 詳細解析にて原因を解明、テストソフトを作成2/5 テストソフトを作成→**ALAP様でも再現試験をお願いします。**■ 2. 技術的な根本原因本不具合は、**I²C通信バッファのデータ破損**が原因です。これは 割込みコンテキストと**OSタスクコンテキスト**の間で同一バッファが保護なしに共有されていることにより発生しています。◆ 関与する実行コンテキスト| コンテキスト              | 役割                                || -----------------------  | ---------------------------------- || 割込みコンテキスト　       | I²C通信完了後にバッファを読み取り、次のトランザクション処理を実施 || OSタスク（5ms / 10ms周期）| 新規のI²C read/write要求を同じバッファへ格納      |◆ 問題の発生メカニズムI²Cバッファが**割込み処理**と**周期タスク処理** の両方から同時にアクセスされているッファに対する**排他制御（ロック機構）が未実装****タイミングが重なった場合：**バッファ内容が破損不正なI²C通信が発生ディスプレイ制御失敗→ **CID/PID ブラックスクリーン発生**■3. 旧リリースで発生しなかった理由| 旧構成              | 結果               || ---------------- | ---------------- || 5msタスクのみで制御      | 次の周期実行前に割込み処理が完了 || バッファは常に空状態へ戻っていた | アクセス競合が発生しなかった   |■ 4. 最新リリースで発生した理由安全アーキテクチャ変更により以下が追加：| 変更点                     | 影響               || ----------------------- | ---------------- || ClusterとCIDを分離          | タスクが2系統化         || CID → 5msタスク            |                  || Cluster → 10msタスク       |                  || 複数タスクがI²C要求を発行          | タイミング競合が発生       || 片方のI²C処理中に別タスクがバッファ書き込み | \*\*レースコンディション発生\*\* |**◆ 失敗シーケンス**10msタスク → I²C要求をバッファへ書き込み        ↓5msタスク起因のI²C完了割込み発生        ↓割込み処理が同バッファを参照・更新        ↓バッファ破損        ↓I²C通信異常        ↓CID画面ブラックアウト**■ 5. 不具合の性質****分類：レースコンディション****原因：共有リソースの排他制御不足****特徴：タイミング依存の間欠不具合****ハード故障ではなく \*\*純粋なソフトウェア設計上の問題****■ 6. 対策方針**I²Cバッファに対してOS標準リソースによる排他制御を実装対策内容：OSタスクがI²C要求を書き込む際にリソースロックを取得割込み処理との同時アクセスを防止バッファ操作をアトミック化**期待効果：**バッファ破損防止I²C通信の安定化ブラックスクリーン事象の恒久対策■ 7. 検証ソフトテスト用デバッグSW`7\\\\\\\_debugSW\\\\\\\_CID\\\\\\\_BLACKSCREEN\\\\\\\_NOI2COMM.zip`**目的：** 排他制御実装の効果確認 不具合再発防止の検証■ 結論本不具合はディスプレイやハードウェア起因ではなく、タスク分割後に顕在化したI²C共有バッファの競合（レースコンディション）\*\* によるソフトウェア不具合です。OSレベルでの排他制御導入により根本的な解決が可能と判断しています。